<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="mobileUserMapper">

	<cache type="org.mybatis.caches.oscache.OSCache"/>


	<delete id="deleteById" parameterType="int">
		delete from mobile_user where id
		= #{id}
	</delete>

	<delete id="deleteByIds" parameterType="java.util.List">
		delete from mobile_user where id in
		<foreach collection="list" index="index" item="id" open="("
			separator="," close=")">
			#{id}
		</foreach>
	</delete>

	<update id="update" parameterType="mobileUser" >
		update mobile_user
		<set>
			realname=#{realname},
			password=#{password},
			phone=#{phone},
			gender=#{gender},
			avatar=#{avatar},
			email=#{email},
			updatetime = #{updatetime}
		</set>
		where id = #{id}
	</update>

	<select id="getById" parameterType="int" resultType="mobileUser" useCache="false">
		select *
		from mobile_user where id = #{id}
	</select>
	
	<select id="getList" parameterType="mobileUser" resultType="mobileUser" useCache="false">
		select
		*
		from mobile_user 
		<where>
			1=1 
			<if test="loginname!=null and loginname!=''">
				and loginname like concat('%',#{loginname},'%')
			</if>
			<if test="phone!=null and phone!=''">
				and phone like concat('%',#{phone},'%')
			</if>
		</where>
		order by updatetime desc;
	</select>
	<select id="getCount" parameterType="mobileUser" resultType="long" useCache="false">
		select count(*) from mobile_user 
		<where>
			1=1 
			<if test="loginname!=null and loginname!=''">
				and loginname like concat('%',#{loginname},'%')
			</if>
			<if test="phone!=null and phone!=''">
				and phone like concat('%',#{phone},'%')
			</if>
		</where>
	</select>
	
</mapper>